import { imageHelper } from 'ember-imdb-search/helpers/image-helper';
import { module, test } from 'qunit';

module('Unit | Helper | image helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageHelper([42]);
  assert.ok(result);
});
