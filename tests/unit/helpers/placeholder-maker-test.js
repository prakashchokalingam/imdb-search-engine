import { placeholderMaker } from 'ember-imdb-search/helpers/placeholder-maker';
import { module, test } from 'qunit';

module('Unit | Helper | placeholder maker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = placeholderMaker([42]);
  assert.ok(result);
});
