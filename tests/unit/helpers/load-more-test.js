import { loadMore } from 'ember-imdb-search/helpers/load-more';
import { module, test } from 'qunit';

module('Unit | Helper | load more');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loadMore([42]);
  assert.ok(result);
});
