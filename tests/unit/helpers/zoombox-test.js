import { zoombox } from 'ember-imdb-search/helpers/zoombox';
import { module, test } from 'qunit';

module('Unit | Helper | zoombox');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = zoombox([42]);
  assert.ok(result);
});
